# Telegram Video Downloader & Uploader

هذا المشروع يقوم بتنزيل حلقات مسلسلات من موقع 3seq، ضغطها إلى 240p، ورفعها إلى قناة Telegram مع دعم التشغيل المتقطع.

## ✨ المميزات
- ✅ استخدام STRING_SESSION للاتصال بـ Telegram
- ✅ ضغط الفيديوهات إلى 240p
- ✅ حذف الملفات بعد الرفع لتوفير المساحة
- ✅ دعم التشغيل المتقطع (يتوقف عند الخروج من الفيديو)
- ✅ العمل على GitHub Actions
- ✅ تكوين المسلسل عبر ملف JSON

## 🔧 المتطلبات

### على GitHub:
1. **مفاتيح الأمان (Secrets):**
   - `API_ID`: معرّف تطبيق Telegram
   - `API_HASH`: التجزئة لتطبيق Telegram
   - `CHANNEL`: معرف القناة (مثال: `@my_channel`)
   - `STRING_SESSION`: جلسة Pyrogram

2. **كيفية الحصول على STRING_SESSION:**
   ```python
   from pyrogram import Client
   
   app = Client(
       "my_session",
       api_id=YOUR_API_ID,
       api_hash=YOUR_API_HASH
   )
   
   with app:
       print(app.export_session_string())
