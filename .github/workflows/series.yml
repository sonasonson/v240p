name: 📺 Series Uploader

on:
  workflow_dispatch:
    inputs:
      series_name:
        description: '📺 اسم المسلسل (إنجليزي)'
        required: true
        default: 'afili-ask'
      series_name_arabic:
        description: '📺 اسم المسلسل (عربي)'
        required: true
        default: 'الحب ورطة'
      season_num:
        description: '🎬 رقم الموسم'
        required: true
        default: '1'
        type: number
      start_episode:
        description: '📈 الحلقة الأولى'
        required: true
        default: '13'
        type: number
      end_episode:
        description: '📉 الحلقة الأخيرة'
        required: true
        default: '15'
        type: number

jobs:
  upload-series:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
    
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: ⚙️ Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg python3-pip
    
    - name: 📦 Install Python dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: 📺 إعداد المسلسل
      run: |
        echo "📺 إعداد ملف المسلسل..."
        
        cat > series_config.json << 'EOF'
        {
          "series_name": "${{ github.event.inputs.series_name }}",
          "series_name_arabic": "${{ github.event.inputs.series_name_arabic }}",
          "season_num": ${{ github.event.inputs.season_num }},
          "start_episode": ${{ github.event.inputs.start_episode }},
          "end_episode": ${{ github.event.inputs.end_episode }}
        }
        EOF
        
        echo "✅ تم إنشاء series_config.json"
        cat series_config.json
        
        EPISODES_COUNT=$((${{ github.event.inputs.end_episode }} - ${{ github.event.inputs.start_episode }} + 1))
        
        echo ""
        echo "📋 معلومات المسلسل:"
        echo "📺 المسلسل: ${{ github.event.inputs.series_name_arabic }}"
        echo "🎬 الموسم: ${{ github.event.inputs.season_num }}"
        echo "📊 عدد الحلقات: $EPISODES_COUNT"
    
    - name: 🚀 تشغيل سكريبت المسلسلات
      env:
        API_ID: ${{ secrets.API_ID }}
        API_HASH: ${{ secrets.API_HASH }}
        CHANNEL: ${{ secrets.CHANNEL }}
        STRING_SESSION: ${{ secrets.STRING_SESSION }}
      run: |
        echo "📺 بدء رفع المسلسل..."
        python main.py 2>&1 | tee processing.log
