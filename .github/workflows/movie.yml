name: 🎬 Movie Uploader

on:
  workflow_dispatch:
    inputs:
      watch_url:
        description: '🔗 رابط مشاهدة الفيلم'
        required: true
        default: 'https://example.com/watch/movie'
      movie_name_arabic:
        description: '📽️ اسم الفيلم (عربي)'
        required: true
        default: 'اسم الفيلم'
      movie_name_english:
        description: '📽️ اسم الفيلم (إنجليزي)'
        required: false
        default: 'Movie Name'

jobs:
  upload-movie:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
    
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: ⚙️ Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg python3-pip
    
    - name: 📦 Install Python dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: 📽️ إعداد الفيلم
      run: |
        echo "🎬 إعداد ملف الفيلم..."
        
        cat > movie_config.json << 'EOF'
        {
          "watch_url": "${{ github.event.inputs.watch_url }}",
          "movie_name_arabic": "${{ github.event.inputs.movie_name_arabic }}",
          "movie_name_english": "${{ github.event.inputs.movie_name_english }}"
        }
        EOF
        
        echo "✅ تم إنشاء movie_config.json"
        cat movie_config.json
    
    - name: 🚀 تشغيل سكريبت الأفلام
      env:
        API_ID: ${{ secrets.API_ID }}
        API_HASH: ${{ secrets.API_HASH }}
        CHANNEL: ${{ secrets.CHANNEL }}
        STRING_SESSION: ${{ secrets.STRING_SESSION }}
      run: |
        echo "🎬 بدء رفع الفيلم..."
        python movie_uploader.py 2>&1 | tee processing.log
