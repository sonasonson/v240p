name: 🎬 Movie Uploader (Direct Links)

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: '🔗 رابط الفيديو المباشر (MP4)'
        required: true
        default: 'https://example.com/video.mp4'
      movie_name_arabic:
        description: '📽️ اسم الفيلم (عربي)'
        required: true
        default: 'اسم الفيلم'
      movie_name_english:
        description: '📽️ اسم الفيلم (إنجليزي)'
        required: false
        default: ''

jobs:
  upload-movie:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
    
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: ⚙️ Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg wget python3-pip
        pip install pyrogram tgcrypto
    
    - name: 📽️ إعداد الفيلم
      run: |
        echo "🎬 إعداد ملف الفيلم..."
        
        cat > movie_config.json << 'EOF'
        {
          "watch_url": "${{ github.event.inputs.video_url }}",
          "movie_name_arabic": "${{ github.event.inputs.movie_name_arabic }}",
          "movie_name_english": "${{ github.event.inputs.movie_name_english }}"
        }
        EOF
        
        cat movie_config.json
    
    - name: 🚀 تشغيل السكربت
      env:
        API_ID: ${{ secrets.API_ID }}
        API_HASH: ${{ secrets.API_HASH }}
        CHANNEL: ${{ secrets.CHANNEL }}
        STRING_SESSION: ${{ secrets.STRING_SESSION }}
      run: |
        python simple_movie_uploader.py
